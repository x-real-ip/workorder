#!/usr/bin/python

# import sqlite3


# con = sqlite3.connect("/app/db/database.db")
# cur = con.cursor()
# cur.execute(
#     "select * from workdays WHERE date=? ORDER BY id DESC LIMIT 1", ("2021-12-08",))
# result = (cur.fetchall())
# con.close()
# print(result)

print("test cron python")
